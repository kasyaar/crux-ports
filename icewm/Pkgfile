name=icewm
version=1.4.2
source=(https://github.com/bbidulock/icewm/releases/download/$version/$name-$version.tar.bz2)
release=1
build () {
    cd $name-$version
    ./configure --prefix=/usr
    make
    make DESTDIR=$PKG install
}
